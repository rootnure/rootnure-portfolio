import SectionTitle from "../../components/SectionTitle";

const Projects = () => {
  return (
    <section className="my-12" name="projects">
      <SectionTitle>Projects</SectionTitle>
    </section>
  );
};

export default Projects;
