import SectionTitle from "../../components/SectionTitle";

const Contact = () => {
  return (
    <section className="my-12" name="contact">
      <SectionTitle>Contact Me</SectionTitle>
    </section>
  );
};

export default Contact;
